import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#FF4A3A",
      light: "#FF5C36",
    },
    divider: "#D9DBE9",
  },
});

export default theme;
